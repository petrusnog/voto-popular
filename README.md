# Voto Popular
Voting application, built with Laravel + Tailwind CSS.
## Build Setup
```shell
    composer install
    npm install
    npm run dev
    cp .env.example .env
    docker compose up -d --build
```
